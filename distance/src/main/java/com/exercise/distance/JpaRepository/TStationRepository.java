package com.exercise.distance.JpaRepository;


import com.exercise.distance.Entity.TStation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TStationRepository extends JpaRepository<TStation,String> {
}
