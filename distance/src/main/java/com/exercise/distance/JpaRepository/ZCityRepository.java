package com.exercise.distance.JpaRepository;

import com.exercise.distance.Entity.ZzCity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ZCityRepository extends JpaRepository<ZzCity,String> {
}
